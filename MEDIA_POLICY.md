# Zeta4 Now 미디어 정책

공개 문서에 사용하는 사진은 이 저장소에서 Markdown과 함께 버전 관리한다. 동영상은 저장하거나
임베드하지 않고 공식 원문 링크로만 제공한다.

## 사진

### 저장 위치

사진은 해당 기사 파일과 같은 디렉터리에 기사 `slug`와 동일한 하위 디렉터리를 만들고 저장한다.

```text
news/<topic>/<YYYY>/<MM>/<slug>.md
news/<topic>/<YYYY>/<MM>/<slug>/<file>.webp
```

본문에서는 저장소 상대경로를 사용한다.

```markdown
![사진이 전달하는 내용을 설명하는 대체 텍스트](./<slug>/<file>.webp)

*사진: 제작자 또는 제공자 · 출처: 원문 URL · 라이선스: 사용 조건*
```

다음 조건을 모두 지켜야 한다.

- 직접 제작했거나 공개 저장소 재배포 권리가 확인된 사진만 사용한다.
- 원문에서 볼 수 있다는 이유만으로 언론사나 제3자의 사진을 복제하지 않는다.
- 제작자·제공자, HTTPS 원문 URL, 라이선스 또는 사용 근거를 사진 바로 다음 줄에 표시한다.
- 의미를 설명하는 대체 텍스트를 반드시 작성한다.
- `webp`, `jpg`, `jpeg`, `png`만 사용한다. 실행 가능한 내용을 포함할 수 있는 `svg`는 사용하지 않는다.
- 권장 최대 폭은 1,600px, 권장 최대 용량은 파일당 1MB다.
- 파일명은 영문 소문자와 숫자, 하이픈을 권장한다.
- 발행된 사진을 같은 경로에서 덮어쓰지 않는다. 수정본은 새 파일명으로 추가해 Git 이력을 보존한다.
- 새 사진과 그 사진을 참조하는 기사 변경은 같은 커밋에 포함한다.
- 외부 이미지 URL이나 `data:` URL을 본문에 직접 넣지 않는다.

## 동영상

동영상은 HTTPS 링크만 사용한다.

```markdown
[관련 영상 보기 — 공식 채널](https://www.youtube.com/watch?v=example)
```

다음 항목은 허용하지 않는다.

- 동영상 파일을 저장소에 커밋하는 행위
- `iframe`, `video`, `audio`, `object`, `embed`, `source` 태그
- 자동재생 또는 추적 스크립트가 포함된 플레이어
- 원 게시자를 확인할 수 없는 재업로드 영상 링크

## 발행 전 확인

- 사진의 재배포 권리와 표시 조건을 확인했는가?
- 사진 대체 텍스트와 출처·라이선스를 작성했는가?
- 사진 경로가 `./<slug>/<file>` 형식인가?
- 동영상이 공식 원문의 HTTPS 링크인가?
- 미디어를 제외해도 본문의 핵심 내용을 이해할 수 있는가?
